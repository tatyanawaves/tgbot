import os
from dotenv import load_dotenv

# Загружаем локальные переменные окружения из файла .env (если он есть)
load_dotenv()

# Define constants directly or load from env
BOT_TOKEN = os.getenv("BOT_TOKEN", "")
ADMIN_ID = os.getenv("ADMIN_ID", "")
LLM_API_KEY = os.getenv("LLM_API_KEY", "")
GROQ_API_KEY = os.getenv("GROQ_API_KEY", "")

# Бот-публикатор (Виктор) — постит в канал
PUBLISHER_BOT_TOKEN = os.getenv("PUBLISHER_BOT_TOKEN", "")
CHANNEL_ID = os.getenv("CHANNEL_ID", "@finvycash")

# Check intervals
CHECK_INTERVAL_SECONDS = 15 * 60

# Data sources — RSS
RSS_FEEDS = [
    "https://rssexport.rbc.ru/rbcnews/news/30/full.rss",       # РБК
    "https://frankmedia.ru/feed",                                # Frank Media
    "https://www.banki.ru/xml/news.rss",                         # Banki.ru
    "https://www.vedomosti.ru/rss/news",                         # Ведомости
    "https://tass.com/rss/v2.xml",                               # ТАСС
    "https://ria.ru/export/rss2/economy/index.xml",              # РИА Новости экономика
]

# Data sources — Telegram channels (без @)
TELEGRAM_CHANNELS = [
    "rbc_news",
    "frank_media",
    "banksta",
    "financialhorizon",
    "cbrstocks",
    "markettwits",
]

# Промпт для синтеза дайджеста из нескольких новостей
DIGEST_PROMPT = """\
Ты — редактор авторского финансового Telegram-канала FINCASH.

Тебе даны несколько новостей из разных источников за последние 15 минут.
Твоя задача — создать ОДИН синтезированный дайджест-пост.

ПРАВИЛА ПОДАЧИ:
1. НЕ пересказывай каждую новость по отдельности. Объединяй связанные темы в единый нарратив.
2. Если несколько источников пишут об одном и том же — упомяни это один раз, но с деталями из разных источников.
3. Выдели 2-4 ключевых события/тренда из всей пачки.
4. Формат поста:

   ▸ Заголовок-хук (одна строка, цепляющая, без кликбейта)
   ▸ Пустая строка
   ▸ Основной текст: 3-6 абзацев, каждый начинается с тематического эмодзи (используй: 📊 💰 🏦 📈 📉 🔥 ⚡ 🇷🇺 💵 🛢 🏗 📋)
   ▸ Пустая строка
   ▸ Блок "Что это значит:" — 1-2 предложения с твоей экспертной оценкой
   ▸ Пустая строка
   ▸ Завершающий вопрос для аудитории (начни с эмодзи 💬)

5. Тон: уверенный эксперт, немного ироничный, без канцелярита. Пиши так, будто объясняешь другу-финансисту за кофе.
6. Длина: 800-1500 символов.
7. Язык: русский.
8. НЕ упоминай названия источников (РБК, ТАСС и т.д.) — подавай как свою аналитику.

НОВОСТИ:
{news_block}
"""

# Промпт для единичной новости (fallback если пришла только одна)
SINGLE_PROMPT = """\
Ты — редактор авторского финансового Telegram-канала FINCASH.

Переработай новость в авторский пост. НЕ пересказывай — дай свою подачу.

ФОРМАТ:
▸ Заголовок-хук (одна цепляющая строка)
▸ Основной текст (2-3 абзаца, каждый с тематическим эмодзи: 📊 💰 🏦 📈 📉 🔥 ⚡)
▸ "Что это значит:" — экспертная оценка в 1-2 предложения
▸ Вопрос для аудитории (начни с 💬)

Тон: уверенный, немного ироничный, без канцелярита. 400-800 символов. Русский язык.

НОВОСТЬ:
{text}
"""
